Follow the procedure in `.claude/skills/setup-profile/SKILL.md` for the current
request (ignore its YAML frontmatter). This repo is agent-neutral: the "skill" file
is just a Markdown playbook. Interview me and replace the sample data in `data/` with
my own, keeping ja/en `id`s in sync and not fabricating anything.

$ARGUMENTS
